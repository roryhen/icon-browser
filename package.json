{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.6.1",
    "snowpack": "^3.3.0"
  },
  "dependencies": {
    "@material-icons/svg": "^1.0.9",
    "clipboard": "^2.0.8",
    "svelte": "^3.37.0"
  },
  "browserslist": [
    "> 1% and last 2 versions"
  ]
}
